import { CustomError } from "./custom-error";
export declare class NotAuthaurizedError extends CustomError {
    constructor();
    statusCode: number;
    serializeErrors(): {
        message: string;
    }[];
}
